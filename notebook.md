 

# C++ 学习笔记

---

## 一、数据类型（Data Types）

### 1.1 整型（Integer）

| 类型          | 大小                          | 范围           |
| ------------- | ----------------------------- | -------------- |
| `short`     | 2B                            | -2^15 ~ 2^15-1 |
| `int`       | 4B                            | -2^31 ~ 2^31-1 |
| `long`      | 4B(Win/Linux32) / 8B(Linux64) | -2^31 ~ 2^31-1 |
| `long long` | 8B                            | -2^63 ~ 2^63-1 |

- `short` < `int` <= `long` < `long long`
- `long` 会有跨平台问题，尽量不要使用，而是以 `int32_t` 与 `int64_t` 代替
- `sizeof` → 获取数据类型占用内存大小

```cpp
cout << sizeof(int) << endl;   // → 4
int a = 33;
cout << sizeof(a) << endl;     // → 4
```

### 1.2 实型（浮点型 / Floating Point）

| 类型                 | 大小 | 有效数字 |
| -------------------- | ---- | -------- |
| `float`（单精度）  | 4B   | 7位      |
| `double`（双精度） | 8B   | 15~16位  |

- `float` 声明末尾要加 `f`

```cpp
float f1 = 3.14;     // 3.14(double) → 3.14(float)  有警告
float f1 = 3.14f;    // 3.14(float)  正确
```

- 科学计数法：`float f2 = nem;` → n × 10^m
- 例：`3e-5` = 3 × 10^-5
- 浮点型输出一般只输出 **6位有效数字**

### 1.3 字符型（Character）

```cpp
char ch = 'a';   // 必须用单引号
```

- 存 ASCII 码
- `char` 占 1B，1 个字符
- 类型转换：`(int)ch` → 输出 ASCII 编码

### 1.4 转义字符（Escape Characters）

| 转义字符 | 含义                              |
| -------- | --------------------------------- |
| `\a`   | 警报（007）                       |
| `\b`   | 退格                              |
| `\f`   | 换页                              |
| `\n`   | 换行                              |
| `\r`   | 回车                              |
| `\t`   | 水平制表（统一横向间距）          |
| `\v`   | 垂直制表（统一纵向间距）          |
| `\0`   | 空字符                            |
| `\ddd` | 1~3位八进制字符（0 <= d <= 7）    |
| `\xhh` | 1~2位十六进制字符（无限贪婪匹配） |

### 1.5 字符串型（String）

- **C 风格**：`char 变量名[] = "值";`
- **C++ 风格**：`string 变量名 = "值";` — 需要 `#include <string>`

> 变量的重赋值无需声明类型。

### 1.6 布尔型（Boolean）

- `true` = 1，`false` = 0，占 1B
- **0 为假，非 0 为真**，字母也是真

```cpp
bool b = 0;
```

### 1.7 数据的输入

```cpp
cin >> a;   // 将输入赋值给 a
```

- 输入给 bool：数字非 0 为 1，0 为 0，字母是假

```cpp
bool b = true;
bool a = "sdaf";
cout << a << endl;   // → 1
cin >> a;
cout << a << endl;   // 输入 "dsaf" → 0
```

---

## 二、运算符（Operators）

### 2.1 算术运算符（Arithmetic Operators）

| 运算符 | 说明                           |
| ------ | ------------------------------ |
| `%`  | 取余 — 表达式中不能含有浮点数 |
| `++` | 前/后置递增 +1                 |
| `--` | 前/后置递减 -1                 |

- 整数相除 → 结果仍是整数，直接消除小数部分，不允许除 0
- 小数相除 → 结果可以是小数也可以是整数
- **前置（prefix）**：先自增，再返回修改后的对象
- **后置（postfix）**：先返回原始对象，再自增
- 后置判定优先于前置：`a+++b` 属于后置
- `cout` 直接输入数字会输出八进制数

### 2.2 赋值运算符（Assignment Operators）

`=` `+=` `-=` `*=` `/=` `%=`

### 2.3 逻辑运算符（Logical Operators）

| 运算符 | 含义      |
| ------ | --------- |
| `!`  | 非（NOT） |
| `&&` | 与（AND） |
| `\|\|` | 或（OR）  |

---

## 三、选择结构（Selection Structures）

### 3.1 单行 if

```cpp
if (判断条件) {
    条件正确的执行操作
}
```

> 注意：if 后不用加分号。

### 3.2 多行 if

```cpp
if (条件) {
    满足时的操作
} else {
    不满足时的操作
}
```

### 3.3 多条件 if

```cpp
if (条件1) {
    条件1满足的代码
} else if (条件2) {
    条件2满足的代码
} else if (条件n) {
    条件n满足的代码
} else {
    以上条件都不满足的代码
}
```

### 3.4 嵌套 if

```cpp
if (条件1) {
    条件1满足时的代码
    if (条件2) {
        条件1与条件2同时满足时的代码
    }
}
```

### 3.5 三目运算符（Ternary Operator）

```
表达式1 ? 表达式2 : 表达式3
```

- 1 为真 → 执行 2
- 1 为假 → 执行 3

C++ 中三目运算符返回的是**变量**，可以继续赋值：

```cpp
(a < b ? a : b) = 10;
// a < b → a = 10
// a >= b → b = 10
```

### 3.6 switch 语句

```cpp
switch (表达式) {
    case 结果1: 语句1; break;
    case 结果2: 语句2; break;
    case 结果3: 语句3; break;
    // ...
    default: 最后执行的语句; break;
}
```

- 无法判断区间，结构相较 if 更清晰

---

## 四、循环结构（Loop Structures）

### 4.1 while 循环

```cpp
while (循环条件) {
    条件为真时执行
}
```

### 4.2 do while 循环

```cpp
do {
    循环语句
} while (循环条件);
```

先执行一次循环语句，再走循环。

### 4.3 for 循环

```cpp
for (起始表达式; 循环条件表达式; 末尾循环体) {
    循环语句
}
```

三种循环基本一样，`for` 更清晰，更常用。

### 4.4 嵌套循环

循环中嵌套另一个循环。

### 4.5 跳转语句（Jump Statements）

| 语句          | 作用                                                                    |
| ------------- | ----------------------------------------------------------------------- |
| `break`     | switch 中终止 case 并跳出；循环中跳出循环；嵌套循环中跳出最近的内层循环 |
| `continue`  | 跳过本次循环未执行的语句，执行下次循环                                  |
| `goto 标记` | 跳转到标记的位置执行                                                    |

---

## 五、数组（Arrays）

> 一个存放了**相同类型**数据元素的集合。
>
> 1. 每个数据元素都是相同的数据类型
> 2. 由**连续的内存空间**组成

### 5.1 一维数组

**定义方式：**

```cpp
数据类型 数组名[数组长度];
数据类型 数组名[数组长度] = {值1, 值2, ...};
数据类型 数组名[] = {值1, 值2, ...};
```

- 通过下标访问元素，从 0 开始
- 初始化时如果未填满，剩余用 0 填充
- 数组名：
  - 统计整个数组内存长度：`sizeof(arr)`
  - 元素数：`sizeof(arr) / sizeof(arr[0])`
  - 数组名即为首地址：`cout << arr << endl;` → 输出十六进制数
  - 数组名是**常量**，不可赋值

### 5.2 二维数组

**定义方式：**

```cpp
数据类型 数组名[行数][列数];
数据类型 数组名[行数][列数] = {{数据1, 数据2}, {数据3, 数据4}};
数据类型 数组名[行数][列数] = {数据1, 数据2, 数据3, 数据4};
数据类型 数组名[][列数] = {数据1, 数据2, 数据3, 数据4};
```

- 内行外列，先行后列
- 行数可以省略，列数不行
- 数组名：
  - 总大小：`sizeof(arr)`
  - 行数：`sizeof(arr) / sizeof(arr[0])`
  - 列数：`sizeof(arr[0]) / sizeof(arr[0][0])`

### 5.3 冒泡排序（Bubble Sort）

**升序步骤：**

1. 比较相邻元素，若前比后大，则交换顺序
2. 对每个元素做同样工作，找出第一个最大值
3. 重复以上步骤，每次比较次数减一，直到不需要比较

---

## 六、函数（Functions）

> 将经常使用的代码封装，减少重复代码。

### 6.1 定义

```cpp
返回值类型 函数名(参数列表) {
    函数体
    return 返回值;
}
```

- 函数不允许嵌套定义

### 6.2 调用

```cpp
函数名(参数);
```

### 6.3 值传递（Pass by Value）

函数调用时，实参将值传入给形参，形参改变，不影响实参。

### 6.4 声明（Declaration）

当函数定义在主函数后面时无法被调用，在代码开头声明函数即可：

```cpp
返回值类型 函数名(参数列表);
// 例：
int func(int a1, int a2);
```

> 可以多次声明，但只能有一次定义。

### 6.5 函数的分文件编写

1. 创建 `.h` 头文件
2. 创建 `.cpp` 源文件
3. 在头文件中写函数的声明
4. 在源文件中写函数的定义
5. 在头文件中 `#include` 源文件
6. 在主文件中 `#include` 头文件

> 自建库用双引号 `"xxx.h"` 而非尖括号。

### 6.6 默认参数（Default Parameters）

```cpp
返回值类型 函数名(类型 参数 = 默认值);
```

- 有默认值的参数只能放在所有无默认值参数的**后面**
- 声明和实现中不能同时有默认参数

### 6.7 占位参数（Placeholder Parameters）

只有数据类型没有名字的参数，可以给默认值。

### 6.8 函数重载（Function Overloading）

> 函数名相同，提高复用性 — 针对不同的个体执行同样的操作，但描述一致。

**条件：**

1. 同一作用域
2. 函数名相同
3. 函数参数的**类型、个数、顺序**不同

**注意事项：**

- 引用/指针作为重载条件时，`const` 也算类型不同，但带 `const` 的形参函数只在传带 `const` 的实参时被调用
- 重载时有默认参数 → 可能出现**二义性（ambiguity）**
- 函数的**返回值**不可作为函数重载的条件

---

## 七、指针（Pointers）

> 可通过指针间接访问内存。

- 内存一般以十六进制表示
- 可利用指针变量保存地址
- `&变量名` 表示取地址

### 7.1 定义与使用

```cpp
// 定义
数据类型 * 指针名;

// 赋值
指针名 = &变量名;

// 定义 + 赋值
数据类型 * 指针名 = &变量名;

// 使用
p               // 直接得到所指内存地址
*p              // 解引用（dereference）：访问内存中的数据
*p = ...        // 解引用：修改内存中的数据
```

### 7.2 指针所占内存空间

- 32位系统：4 字节
- 64位系统：8 字节

### 7.3 空指针（Null Pointer）vs 野指针（Wild Pointer）

| 类型             | 说明                                    |        打印        |          解引用          |
| ---------------- | --------------------------------------- | :-----------------: | :----------------------: |
| **空指针** | 指向内存编号为 0 的空间，一般用于初始化 | **True** 合法 | **False** 程序崩溃 |
| **野指针** | 指向非法的内存空间                      | **True** 合法 | **False** 程序崩溃 |

> **辨析**：空指针可以检查其是否可用，是可控的；野指针无法检查，不可控。

### 7.4 const 修饰指针

| 写法                         | 名称       | 含义                                           |
| ---------------------------- | ---------- | ---------------------------------------------- |
| `const int* p = &a;`       | 常量指针   | `const` 修饰 `*`，`*p`（指向的值）不能改 |
| `int* const p = &a;`       | 指针常量   | `const` 修饰 `p`，`p`（指向）不能改      |
| `const int* const p = &a;` | 两者都修饰 | 两者都不能改                                   |

### 7.5 指针与数组

- 数组名就是其首地址
- `arr[3]` = `*(arr + 3)` — 对数组指针索引等同于解引用索引处的地址
- 指针的运算以其指向的数据类型大小为单位
- 对数组名取地址 `&arr`，将获取整个数组的地址（数值上等于首地址，但指针运算时会按整个数组为单位移动）
- `sizeof` 指向数组的指针会得到指针的长度（而非数组长度）

### 7.6 指针与函数

**值传递** — 无法改变实参：

```cpp
void swap01(int a, int b) { ... }
swap01(a, b);   // a, b 未交换
```

**地址传递** — 可通过指针改变实参的值：

```cpp
void swap02(int* a, int* b) { ... }
swap02(&a, &b);  // a, b 已交换
```

> **const 保护**：值传递会占用过多内存时使用地址传递，为防止误操作，可在形参处加 `const`。

---

## 八、结构体（Structs）

> 用户自定义的数据类型，允许存储不同的数据类型。

### 8.1 定义与创建

```cpp
struct 结构体名 { 结构体成员列表 };   // 每个成员用分号隔开

// 创建变量的三种方式：
struct 结构体名 变量名;
struct 结构体名 变量名 = {成员1值, 成员2值, ...};
struct 结构体名 { ... } 变量名;          // 定义时顺便创建
```

- 用 `.` 访问属性
- 指针用 `->` 访问属性，或 `(*指针).属性`

### 8.2 结构体数组

```cpp
struct 结构体名 数组名[长度] = {
    {成员1值, 成员2值, ...},
    {成员1值, 成员2值, ...},
    ...
};

// 访问
数组名[序列].属性
```

### 8.3 结构体嵌套

```cpp
struct Student {
    string name;
    int id;
    int score;
};

struct Classes {
    int number;
    struct Student stu;
};
```

嵌套的结构体定义时只能按成员的属性依次定义，无法直接用结构体定义。

```cpp
// 创建嵌套结构体数组
struct Student stu1 = { "mace", 111, 666 };
struct Classes class1[2] = {
    { 1, stu1.name, stu1.id, stu1.score }
};
```

### 8.4 结构体作函数参数

| 方式     | 函数定义                        | 传参                    |
| -------- | ------------------------------- | ----------------------- |
| 值传递   | `void func(结构体名 形参名)`  | `func(结构体变量名)`  |
| 地址传递 | `void func(结构体名* 形参名)` | `func(&结构体变量名)` |

---

## 九、内存四区（Memory Layout）

> 在不同区域存放的数据，赋予不同的生命周期，可提高编程的灵活度。

编译生成 exe 后，未执行程序前分为两个区域：

**代码区（Code Area）**：存放函数体的二进制代码，由操作系统管理

- 共享（Shared）— 频繁调用的程序只需一份代码
- 只读（Read-only）— 防止程序意外修改指令

**全局区（Global Area）**：存放全局变量、静态变量及常量，程序结束后由操作系统释放

| 存储内容                    |      是否在全局区      |
| --------------------------- | :---------------------: |
| 全局变量（函数外声明）      |     **True**     |
| 静态变量（`static` 修饰） |     **True**     |
| 字符串常量                  |     **True**     |
| 全局常量（`const` 全局）  |     **True**     |
| 局部变量                    | **False**（栈区） |
| 局部常量（`const` 局部）  | **False**（栈区） |

> `const` 仅表示"只读"，与内存区无关。

**栈区（Stack）**：由编译器自动分配释放，存放函数参数值、局部变量、局部常量等

- **! 不要返回栈区数据的地址，系统会直接释放**

**堆区（Heap）**：由程序员分配和释放，若程序员不释放，程序结束时由操作系统回收

```cpp
// 在堆区开辟内存
int* p = new int(10);    // new 返回对应指针
delete p;                // 释放内存

// 释放数组
int* arr = new int[10];
delete[] arr;            // 加中括号
```

## 十、引用（Reference）

> 两个指向同一地址的变量名 — 别名（alias）。

```cpp
数据类型 &别名 = 原名;
```

```cpp
int a = 10;
int &b = a;
b = 30;
cout << a << endl;   // → 30
cout << &a << endl;  // → 同一地址
cout << &b << endl;  // → 同一地址
```

* **! 引用必须初始化（必须引用一块合法的内存空间）**
* **! 引用初始化后不可更改（后面 `b = a` 是赋值，不是更改引用）**

**引用作函数参数** — 可改变实参，比指针简化：

```cpp
void swap(int &a, int &b) {
    int temp = a;
    a = b;
    b = temp;
}
```

**引用作函数返回值**：

- 函数返回引用时，函数调用可作为左值进行赋值
- **! 不要返回局部变量的引用（第二次调用会被清掉）**

**引用的本质**：一个**指针常量**（指向固定地址的指针）

**常量引用** — 用于修饰形参，防止误操作：

```cpp
const int& ref = 10;
// 等价于：int temp = 10; const int& ref = temp;
```

**`&` 与 `*` 的区别：**

| 场景   | `&`                  | `*`                      |
| ------ | ---------------------- | -------------------------- |
| 声明时 | 声明别名（同一块内存） | 声明指针（保存地址的变量） |
| 使用时 | 取地址                 | 解引用（按地址取值）       |

## 十一、类与对象（Class & Object）

> 万物皆对象，对象有**属性（property）**和**行为（behavior）**。
> 具有相同性质的对象，可以抽象为**类（class）**。

### 11.1 struct 与 class 的区别

- 都可以用于写类，唯一区别在于**默认访问权限**不同
- `struct`：默认 `public`
- `class`：默认 `private`
- **!**  一般习惯上 `struct` 不写复杂逻辑

### 11.2 封装（Encapsulation）

**意义：**

1. 将属性和行为作为整体，表现生活中的事物
2. 将属性与行为加以权限控制

**语法：**

```cpp
class 类名 {
    访问权限:
        属性 / 行为
};
```

#### 1. 权限管理（Access Control）

| 权限          |      类内      |      类外      |      子类      |
| ------------- | :------------: | :-------------: | :-------------: |
| `public`    | **True** | **True** | **True** |
| `protected` | **True** | **False** | **True** |
| `private`   | **True** | **False** | **False** |

**属性设置为私有的好处：**

- 检测数据的有效性
- 自己管理读写权限

### 11.3 对象特性

#### 1. 构造函数（Constructor）

C++ 会自动调用构造函数和析构函数，若我们不提供构造与析构，编译器会提供空实现的构造和析构。

**语法：** `类名(){}`

1. 没有返回值，不写返回值类型
2. 函数名与类名相同
3. 可以有参数，所以也可以重载
4. 调用对象时会自动调用构造函数，且只会调用一次

**分类：**

| 按参数分             | 按类型分 |
| -------------------- | -------- |
| 有参构造             | 普通构造 |
| 无参构造（默认构造） | 拷贝构造 |

**拷贝构造：**

```cpp
Person(const Person &p);   // 参数是本类类型，只读，引用
```

**拷贝构造函数调用的时机：**

1. 使用一个创建完毕的对象来初始化一个新对象
2. 用值传递给函数参数传值 — 函数值传递方式以对象作参数时，函数实际上会拷贝出一个对象在函数内部使用
3. 以值方式返回局部对象 — 局部对象作函数返回值时也会拷贝后传出

**浅拷贝与深拷贝：**

- **浅拷贝（Shallow Copy）**：简单赋值拷贝。如果在堆区开内存保存数据，析构时释放，浅拷贝会把同一地址拷贝过去，可能出现重复释放的问题
- **深拷贝（Deep Copy）**：在堆区申请空间进行拷贝操作，再开一个新的内存存拷贝数据

> 若属性有在堆区开辟的，要自己提供拷贝构造函数，防止浅拷贝的问题。

**调用方式：**

| 方式       | 示例                     | 注意                                                                                                              |
| ---------- | ------------------------ | ----------------------------------------------------------------------------------------------------------------- |
| 括号法     | `Person p1;`           | 无参构造函数不加括号，会被认为是函数声明                                                                          |
| 显示法     | `Person p = Person();` | 类似 Python；匿名对象`Person(10);` 在当前行执行结束后立即回收；不要用拷贝构造初始化匿名对象，会被识别成对象声明 |
| 隐式转换法 | `Person p4 = 10;`      | `Person p4 = 10` → `Person p4 = Person(10)`                                                                  |

**调用规则：**

1. 默认情况下编译器会给空的默认构造、析构函数，会给一个对属性进行值拷贝的默认拷贝函数
2. 若有用户定义的有参构造函数，则无无参默认，但会给默认拷贝函数
3. 若定义了拷贝构造函数，则 C++ 不会再提供其他构造函数

**初始化列表：**

```cpp
构造函数(): 属性1(值1), 属性2(值2) { }
// 灵活初始化
构造函数(a, b, c): 属性1(a), 属性2(b) { }
```

不用在构造函数内再赋值，可以直接赋值给属性，会直接赋予类定义中的值。灵活初始化可在对象中各自赋值。

#### 2. 析构函数（Destructor）

**语法：** `~类名(){}`

1. 无返回值，不写返回值类型
2. 函数名与类名相同，在名称前加 `~`
3. 不可以有参数，因此不可以重载
4. 程序在对象销毁前会自动调用，且只会调用一次

> 有 `system("pause")` 时，main 中释放的对象析构函数会在关闭的瞬间运行。

#### 3. 类对象作为类成员

一个类的成员可以是另一个类的对象，称为**对象成员**（member object）。

```cpp
class A { };
class B {
    A a;   // 对象成员
};
```

**构造与析构的顺序：**

- 构造：先内后外（先成员对象，再自身）
- 析构：先外后内（先自身，再成员对象）

#### 4. 静态成员（Static Members）

在成员变量和成员函数前加 `static`，是同一文件环境下整个类的所有对象的共同之处，有访问权限。

**静态成员变量：**

- 所有对象共享同一份数据
- 编译阶段分配内存
- 类内声明，类外初始化
- 可通过类名或对象名访问（需用作用域解析运算符 `::`）

**静态成员函数：**

- 所有对象共享同一个函数
- 静态成员函数只能访问静态成员变量
- 同样有两种访问方式，类外声明，类内定义

```cpp
class Person {
public:
    static int m_a;          // 类内声明
    static void func() {
        cout << "类静态函数调用" << endl;
    }
};
int Person::m_a = 100;       // 类外初始化

int main() {
    Person a;
    cout << a.m_a << endl;          // 通过对象访问
    cout << Person::m_a << endl;    // 通过类名访问
    return 0;
}
```

#### 5. 成员变量和成员函数分开存储

- 只有非静态成员变量才属于类的对象
- 空对象会分配一个字节空间，为了区分空对象占内存的位置
- 非空对象不会加上一开始的一个字节

#### 6. this 指针

`this` 指针是一个指针常量（pointer constant），指向本对象：`const Person* const this`。

- 无需定义，隐含于非静态成员函数内，可直接调用

**用途：**

1. 形参与成员变量重名时，用 `this` 指针区分：`this->name = name;`
2. 类的非静态函数中返回对象本身，使用 `*this`，可用作链式编程

> 空指针调用的函数中有 `this`，则 `this` 无法找到指向的对象，导致报错。可在使用了 `this` 的函数中对空指针加条件返回。

#### 7. const 修饰成员函数

**常函数：** 在函数名后面加 `const`，使 `this` 指针指向的值也不可修改，因此常函数不能修改成员属性。

**常对象：** 在声明对象前加 `const`。

- 成员属性声明时加关键字 `mutable` 可以在常函数和常对象中修改
- 常对象只能调用常函数，也不能修改属性

**类外实现函数：** 类名前加作用域解析符后正常写。

#### 8. 友元（Friend）

可在类外访问类内私有成员的特殊函数或类，关键字：`friend`。

在类内添加带 `friend` 的声明：

1. 全局函数做友元：`friend void bbb(int arg1);`
2. 类做友元：`friend class AAA;`
3. 成员函数做友元（带作用域）：`friend void AAA::bbb(int arg1);`

#### 9. 运算符重载（Operator Overloading）

对已有的运算符重新定义，以适应不同的数据结构运算。

**通用成员函数重载（类内）：**

```cpp
Person operator+(Person &p) { }
// 调用：
p3 = p1.operator+(p2);
p3 = p1 + p2;
```

**全局函数重载（类外）：**

```cpp
Person operator+(Person &p1, Person &p2) { }
// 调用：
p3 = operator+(p1, p2);
p3 = p1 + p2;
```

**四则运算符重载**、**左移运算符重载**（一般用全局函数重载，与 `cout` 联动需返回 `ostream&`）、**递增/递减重载**（后置加占位参数区分）、**赋值/比较运算符重载**、**函数调用运算符重载**（仿函数）：

```cpp
返回值 operator()(形参列表) {
    // ...
    return ...;
}
```

---

### 11.4 继承（Inheritance）

> 有父级的共性，有自己的特性。

**语法：**

```cpp
class 子类 : 继承方式 父类 { };
```

```cpp
class Language {
public:
    Language() {
        cout << "a language" << endl;
    }
};

class Java : public Language {
public:
    void content() {
        cout << "Java" << endl;
    }
};
```

#### 1. 继承方式

按权限分类，分为**公共继承**、**保护继承**和**私有继承**：

- 父类的私有属性无法继承
- 低于继承权限的父类属性会纳入子类的继承权限，其余权限的父类属性则会按原权限纳入子类权限
- 如以保护权限继承的子类，父类中公共、保护权限的属性共同纳入子类的保护权限，而父类私有权限的属性不继承

#### 2. 继承的对象模型

- 父类的所有非静态成员都会被子类继承
- 但父类的私有成员无法访问
- cmd 可查看对象模型：`cl /d1 reportSingleClassLayout类名 文件名`

#### 3. 构造与析构的顺序

子类创造对象的同时，父类也会创造一个对象：

- 构造：父类构造 → 子类构造
- 析构：子类析构 → 父类析构

#### 4. 父子类重名成员的处理

- 子类的成员可以直接访问
- 父类要加作用域：`子类对象.父类名::成员名;`
- 同名函数不会重写，父子的函数都可以访问
- 父类的同名重载和子类的同名函数没关系，还是要作用域
- 静态和非静态都是，但静态可以用类名访问

#### 5. 多继承（Multiple Inheritance）

```cpp
class 子类 : 继承方式 父类1, 继承方式 父类2, 继承方式 父类3 { };
```

多继承中可能出现同名成员，要用作用域区分。

#### 6. 菱形继承（Diamond Inheritance）

两个派生类继承同一个基类，某个类又同时继承两个派生类，则是菱形继承。

**问题：**

1. 最高父类的东西会出现二义性
2. 最高父类的东西会重复

**解决：** 利用**虚继承（virtual inheritance）**

```cpp
class 子类名 : virtual 权限 父类名 { };
```

父类变成**虚基类**，共有的数据变为同一个。不同的派生类继承统一指针，指针指向一个基类。

#### 7. 父类指针与引用

父类的指针、引用可以指向子类对象（多态的基础）。

---

### 11.5 多态（Polymorphism）

#### 1. 概述

| 类型     | 说明                           |
| -------- | ------------------------------ |
| 静态多态 | 函数重载、运算符重载           |
| 动态多态 | 派生类与虚函数实现运行时的多态 |

**区别：**

- 静态多态：函数地址在**编译阶段**确定
- 动态多态：函数地址在**运行阶段**绑定（基类成员函数定义前加 `virtual`）

#### 2. 动态多态的条件

1. 必须存在**继承关系**
2. 子类**重写（override）** 父类的虚函数（重写：返回值类型、函数名、参数列表一致）
3. 父类的**指针或引用**指向子类的对象时使用

> 底层原理
> 当父类有虚函数时，每个实例都会有 **vfptr**，指向 **vtable**（存储所有虚函数地址）。
> 子类实例创建时，vfptr 指向子类自己的 vtable，其中保存着父类的虚函数。
> 如果子类重写了虚函数，vtable 中会将父类版本替换为子类版本。
> 多态由此实现。
> **! 实例（instance）有 vfptr，类（class）有 vtable。**

#### 3. 代码示例

```cpp
class Animal01 {
public:
    // 使用虚函数
    virtual void speak() {
        cout << "animal01 is talking !" << endl;
    }
};

class Animal02 {
public:
    // 未使用虚函数
    void speak() {
        cout << "animal02 is talking !" << endl;
    }
};

class Cat : public Animal01 {
public:
    void speak() {
        cout << "cat is talking ! " << endl;
    }
};

class Dog : public Animal02 {
public:
    void speak() {
        cout << "dog is talking ! " << endl;
    }
};

void test01(Animal01& animal) {
    animal.speak();
}

void test02(Animal02& animal) {
    animal.speak();
}

int main() {
    Cat cat;
    test01(cat);
    // >>> cat is talking

    Dog dog;
    test02(dog);
    // >>> animal02 is talking

    return 0;
}
```

[Calculator](D:\C\calculator.cpp)   实践示例

优势： 也是此时推荐用多态

1. 结构清晰，可读性强，方便定位问题
2. 易于后续维护，**对修改关闭，对扩展开放**

#### 4. 纯虚函数与抽象类

当父类中的虚函数**毫无意义、无需调用**时，使用纯虚函数

拥有纯虚函数的类，被称为**抽象类**

纯虚函数的语法：

```C++
virtual 返回值类型 函数名(参数列表) = 0;
```

抽象类的特点：

1. 无法实例化对象
2. 子类必须重写父类中的纯虚函数，否则也无法实例化对象

##### 1. 虚析构与纯虚析构

问题：父类指针在析构时不会调用子类析构代码

解决：使用虚析构或纯虚构函数

共性：

* 可以让父类指针释放子类对象
* 要有具体的定义

区别：

* 纯虚构函数会让类变成抽象类，不可实例化

若出现纯虚析构，则该类也属于抽象类，无法实例化对象

语法：

```C++
//虚析构
virtual ~类名(){...}
//虚析构使用编译器默认虚析构
virtual ~类名(){} = default;
  
//纯虚析构
virtual ~类名(){} = 0；
//类外定义
类名::类名(){}
```
