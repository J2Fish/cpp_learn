数据类型

整型
short            2b              -2^15~2^15-1  32768
int              4b              -2^31~2^31-1  
long 4b(win linux32) 8b(linux64) -2^31~2^31-1 
long long        8b              -2^63~2^63-1
short < int <= long <long long 
long 会有跨平台问题，尽量不要使用，而是以int_32t 与 int_64t 代替
sizeof -> 获取数据类型占用内存大小
-cout << sizeof(int) << endl;
->4
-int a = 33;
-cout << sizeof(a) << endl;
->4

实型（浮点型）
float（单精度） 4b  7位有效数字
double（双精度） 8b 15~16位有效数字
float 声明末尾要加f
float f1 = 3.14;  3.14(double)->3.14(float)
float f1 = 3.14f; 3.14(float)
科学计数法表示
float f2 = nem;  n*10^m
3e-5 = 3*10^5
浮点型输出一般只输出6位有效数字

字符型
char ch = 'a';  必须用单引号
存ASCII码
char 1b 1字符
类型转换： (int)ch -> 输出ASCII编码

转义字符
\+某字符
\a 警报 007
\b 退格
\f 换页
\n 换行
\r 回车
\t 水平制表 统一横向间距
\v 垂直制表 统一纵向间距
\0 空字符
\ddd 1~3位8进制的任意数字 /+ddd 至多匹配三位 0<= d <=7
\xhh 1~2位16进制的任意数字 \x+hh… 后面有多少16进制数据匹配多少（无限贪婪） h 0~9+a~f/A~F

字符串型
C风格 char 变量名[] = "值"
C++风格 string 变量名 = "值" C++ 需要包含头文件string #include <string>

变量的重赋值无需声明类型

布尔型
true 1 flase 0  占1b  0为假，非0为真,字母也是真
bool b = 0

数据的输入
cin >> a 将输入赋值给a
输入给bool数字非0为1，0为0，字母是假 
-bool b = true;
-bool a= "sdaf";
-cout << a << endl;
-cin >> a;
-cout << a << endl;
->1
->dsaf
->0

运算符

算术运算符
% 取余 表达式中不能含有浮点数
++ 前/后置递增 +1
-- 前/后置递减 -1 
整数相除，结果依然是整数，直接消除小数部分，不允许除0
小数相除,结果可以是小数也可以是整数
前置：先自增，再返回修改后的对象
后置：先返回原始对象，再自增
后置判定优先与前置
a+++b 属于后置
cout 直接输入数字会输出八进制数

赋值运算符
= += -= *= /= %=

逻辑运算符
!  非 
$$ 与 
|| 或

选择结构

单行if 
if (判断条件){
    条件正确的执行操作
}
注意：if后不用加分号

多行if
if (条件){
    满足时的操作
}
else{
    不满足时的操作
}

多条件if
if (条件1){
    条件1满足的代码
}
else if (条件2){
    条件2满足的代码
}
...
else if (条件n){
    条件n满足的代码
}
else {
    以上条件都不满足的代码
}

嵌套if
if (条件1){
    条件1满足时的代码
    if (条件2){
        条件2与2同时满足时的代码
    }
}

三目运算符
表达式1 ? 表达式2 : 表达式3
1 为真，执行2
1 为假，执行3
c++ 中，三目运算符返回的是变量，可以继续赋值
(a < b ? a : b) = 10 
a < b,则a = 10
a <=b,则b = 10

switch 语句 多条件分支语句 无法判断区间 结构相较if更清晰
case (表达式){
    case 结果1: 语句1;break;
    case 结果2: 语句2;break;
    case 结果3: 语句3;break;
    ...
    default: 最后执行的语句;break;
}

循环结构

while 循环
while (循环条件){
    条件为真时执行
}

do while 循环
do (循环语句) while (循环条件);
先执行一次循环语句，再走循环

for 循环
        0           1                   3
for (起始表达式; 循环条件表达式; 末尾循环体(单次循环末尾执行)){
    循环语句 -> 2
}

三种循环基本一样，for更清晰，更常用

嵌套循环

跳转语句
break 
    switch 终止case，跳出switch
    循环语句 跳出循环
    嵌套循环中，跳出最近的内层循环
continue
    跳过本次循环未执行的语句，执行下次循环
goto 标记
    如果标记存在，执行到goto时，跳转至标记的位置

数组 一个存放了相同类型的数据元素的集合
    1、其中的的每个数据元素都是相通的数据类型
    2、由连续的内存空间组成

    定义法：
        数据类型 数组名[数组长度];
        数据类型 数组名[数组长度] = {值1,值2,...}
        数据类型 数组名[] = {值1, 值2, ...}
    数组有序，可通过下标访问数组中的元素,0开始数
    初始化时如果没有填写完全，则用0填充空白
    数组名
        可统计整个数组内存长度 sizeof(arr)  元素数：sizeof(arr) / sizeof(arr[0])
        就是数组在内存中的首地址 cout << arr << endl; -> 输出一个16进制数 转换为十进制最好用long long ，因为int 在64位会丢失精度
        数组名是常量，不可赋值

    二维数组
        定义法：
            数据类型 数组名[行数][列数];
            数据类型 数组名[行数][列数] = {{数据1,数据2},{数据3,数据4}};
            数据类型 数组名[行数][列数] = {数据1, 数据2, 数据3, 数据4};
            数据类型 数组名[ ][列数] = {数据1, 数据2, 数据3, 数据4};
            内行外列，先行后列
            行数可以省略，列数不行
        数组名 注释参考数组的数组名
            查看数组占用的内存大小 行数：sizeof(arr) / sizeof(arr[0]) 列数： sizeof(arr[0]) / sizeof(arr[0][0])
            查看二维数组的首地址 
            

冒泡排序
升序
    比较相邻元素，若前比后大，则交换顺序
    对每个元素做同样工作，找出第一个最大值
    重复以上步骤，每次比较次数减一，直到不需要比较

函数 将经常使用的代码封装，减少重复代码

    定义 
    回值类型 函数名(参数列表)
    {
        函数体
        return 返回值
    }
    函数不允许嵌套

    调用
    函数名(参数)

    值传递
    函数调用时，实参将值传入给形参，形参改变，不影响实参

    声明  当函数的定义在主函数的后面时无法被调用，在代码开头声明函数可调用函数
        格式：  回值类型 函数名(参数列表);
                int func(int a1, int a2);      

    函数的分文件编写
        1、创建后缀名为.h的头文件
        2、创建后缀名为.cpp的源文件
        3、在头文件中写函数的声明
        4、在源文件中写函数的定义
        5、在头文件中include源文件
        6、在主文件中include头文件
        自建库要打引号

    可以多次声明，但只能有一次定义
    函数的参数及返回值都可以没有 无返回值的回值类型写void ，不写调试会警告，但程序可以跑

    默认参数
        在参数后面赋值 
        有默认值的参数只能在所有无默认值的参数后面
        声明和实现中不能同时有默认参数 

    占位参数 ！用处后面补
        没名只有数据类型的参数
        可以给默认值

    函数重载 函数名相同，提高复用性 -> 可以在相同的函数名下，在输入不同的参数的情况下执行不同的操作 -> 针对不同的个体执行同样的操作，但描述时一致
        条件：
            1、统一作用域
            2、函数名相同
            3、函数参数的类型，个数，顺序不同
            ！引用/指针 作为重载条件时的注意事项：
                此时只读也算类型不同 但带const的形参函数只在传带const的实参时被调用
            ！重载时有默认参数的情况 -> 若 两函数在输入完 无/少 默认参数的函数的参数后都可以运行则出现 二义性 -> 可认为条件3中的“参数”实际为“无默认值的参数”
        ！函数的返回值不可作为函数重载的条件



指针 可通过指针间接访问内存
    内存一般以十六进制表示
    可利用指针变量保存地址
    &变量名 表示取地址
    1、定义
    数据类型 * 指针名;
    2、赋值
    指针名 = &变量名;
    数据类型 指针名 = &变量名;

    3、使用
        1、直接得到所指内存 p
        2、解引用 访问内存中的数据 *p
        3、解引用 修改内存中的数据 *p = ...
    4、指针所占的内存空间
        在32位系统下，占用4个字节
        在64为系统下，占用8个字节
    
    空指针 指针变量指向内存中编号为0的空间
        一般用于初始化指针变量
        空指针指向的内存无法访问 0~255的内存编号为系统占用,不可访问 -> 即不可解引用 
            打印空指针是合法的,但解引用空指针不合法
            解引用空指针 编辑器检查不出语法错误,但不能跑,程序会崩溃
            
    野指针 指针变量指向非法的内存空间
        打印野指针是合法的,但解引用空指针不合法
        解引用野指针 编辑器检查不出语法错误,但不能跑,程序会崩溃

    空/野辨析
        空指针可以检查其是否可用、是否是一个正常指针,而野指针无法检查
        即空指针是可控的,而野指针并不可控
    const 修饰指针
        1、const修饰指针 -> 常量指针
        const int* p = &a;
        const 修饰到*，则*p不能改 *p即指向的值
        2、const修饰常量 -> 指针常量
        int* const p = &a;
        const 修饰到p，则p不能改 p即指向
        3、const即修饰指针，又修饰常量
        const int* const p = &a;
        const 即修饰到* ，又修饰到p， 则两者都不能改
    
    书+： int* 为复合类型，指 “指向int 的指针”，而*前面的数据类型即为指针指向的数据类型
    指针的运算以其指向的数据类型大小为单位
    
    指针与数组
        数组名就是其首地址
        仅用数组名定义时指针指向其首项的地址，后续移动中也只会指向其某项的地址
        对数组名取地址，将获取整个数组的地址 （数值上等于首地址，但在指针的后续操作中会按数组为单位进行）
        arr[3] = *(arr + 3) -> 对 「数组指针」 索引 等同于 「解引用」 索引处的地址
        sizeof 指向数组的指针 会得到指针的长度
    
    指针与函数
        1、值传递 无法改变实参
        int a = 0;
        int b = 1;
        swap01(a, b);
        cout << "a:" << a 
             << "\t"
             << "b:" << b 
             <<endl; //未交换
    
        2、地址传递 可通过指针改变实参的值
        swap02(&a, &b);
        cout << "a:" << a 
             << "\t"
             << "b:" << b 
             <<endl; //已交换
        
        const 
            值传递会占用过多内存，此时使用地址传递，
            为防止误操作导致在内部修改，可在形参定义处+const

结构体  用户自定义的数据类型，允许用户存储不同的数据类型
    定义
        struct 结构体名 {结构体成员列表}; 每个成员用分号隔开
        通过结构体创建变量的方式
            1、 struct 结构体名 变量名
            2、 struct 结构体名 变量名 = {成员1值, 成员2值, ...}
            3、 定义结构体时顺便创建变量 -> 大括号后面写变量值
        用“.”访问结构体变量中的属性
        设定属性值
            1、 struct 结构体名 变量名 = {成员1值, 成员2值, ...}
            2、 变量名.属性 = 成员值
        指针
            用 变量名 -> 属性 访问属性
            或 *变量名.属性
    结构体数组
        需要在定义结构体后定义
        定义 
            struct 结构体名 数组名[长度] = {
                {成员1值, 成员2值, ...},
                {成员1值, 成员2值, ...},
                {成员1值, 成员2值, ...},
                ...
            };
        访问 
            数组名[序列].属性
        指针
            对结构体数组执行 如arr[9] 的操作，也视为 *(arr + 9)
    结构体嵌套
        结构体的成员可以是结构体
        嵌套的结构体定义其成员结构体时，只能按成员的属性依次定义，无法直接用结构体定义
        嵌套语法
            struct Classes {
                int number;
                struct Student stu;
            };

            struct Student {
                string name;
                int id;
                int score;
            };

            struct Student  stu1 = {
                "mace", 111, 666
            };
        定义语法
            struct Classes class1[2] = {
                {1, stu1.name, stu1.id, stu1.score}
            };
        
        结构体作函数参数 跟前面的传参差不多
            值传递
                函数定义
                    void func(结构体名 形参名){

                    }
                传参
                    func(结构体变量名)
            地址传递  
                函数定义
                    void func(结构体名* 形参名){

                    }
                传参
                    func(&结构体变量名)
            

面向对象

内存四区 代码区，全局区，栈区，堆区     
    在不同区域存放的数据，赋予不同的生命周期，可提高编程的灵活度
    编译生成exe后，未执行程序前被分为两个区域 - 代码区与全局区
        代码区： 存放函数体的二进制代码，由操作系统管理
            共享 - 对于频繁调用的程序，只需有一份代码 
            只读 - 防止程序以外修改了指令
        
        全局区：存放全局变量和静态变量以及常量，在程序结束后由操作系统释放
            全局变量->主函数外声明的变量
            静态变量->static 修饰声明的变量
            常量 -> 分为字符串常量和全局常量，    局部常量（局部常量不在全局区存储）
            代码示例
                main函数外
                    int g_a = 10; 
                    const int c_g_a = 10;
                main函数内
                    //全局区
                    //全局变量->主函数外，静态变量，常量
                    //普通的局部变量
                    int a = 10;
                    int b = 10;
                    cout << "局部变量" << endl;
                    cout << "&a = " << (long long)&a << endl;
                    cout << "&b = " << (long long)&b << endl;
                    cout << "全局变量" << endl;
                    cout << "&g_a = " << (long long)&g_a << endl; 
                    //静态变量
                    static int s_a = 10;
                    cout << "静态变量" << endl;
                    cout << "&s_a = "  << (long long)&s_a<< endl; 
                    //常量 - 分为字符串常量和全局常量，局部常量 
                    cout << "字符串常量" << endl;
                    cout << "&'sdafsadf' = "  << (long long)&"sdafsadf" << endl;
                    //全局常量-const修饰的全局变量，局部常量则是const修饰的局部变量
                    const int c_a = 10;
                    cout << "局部常量" << endl;
                    cout << "&c_a = " << (long long)&c_a << endl;
                    cout << "全局常量" << endl;
                    cout << "&c_g_a = " << (long long)&c_g_a << endl;
                    cout << "由此可知，全局变量，静态变量，常量(不含局部常量)在内存中位于同一区域（全局区） const仅表示“只读”，与内存区无关" << endl;
                
                输出：
                局部变量
                &a = 6291068
                &b = 6291064
                全局变量
                &g_a = 140700647829504
                静态变量
                &s_a = 140700647829508
                字符串常量
                &'sdafsadf' = 140700647833702
                局部常量
                &c_a = 6291060
                全局常量
                &c_g_a = 140700647833600
                由此可知，全局变量，静态变量，常量在内存中位于同一区域

            
    
    栈区：由编译器自动分配释放，存放函数参数值，局部变量，局部常量等
        ！不要返回栈区数据的地址，系统会直接释放
        返回的第一次调用会有暂时的保留，后面就没了

    堆区：由程序员分配和释放，若程序员不释放，组程序结束时由操作系统回收 实则存储形式类似于链表
        在C++中主要用new在堆区开辟内存，用delete释放内存
            指针本质为局部变量存在栈区（地址名存在栈区），但指针保存的数据在堆区（地址和上面的数据在堆区）
            代码示例：
                main内
                    //在堆区开辟内存
                    int* p = func();
                    cout << *p << endl;
                    cout << *p << endl;
                    cout << *p << endl;
                    delete p;
                    cout << *p << endl;
                main外
                    int* func(){
                        int* p = new int(10); //new 修饰返回对应指针
                        return p;
                    }
                输出：
                    10
                    10
                    10
                    8027664
        在释放数组时，要加中括号：
            delete[] arr;


引用 两个指向同一地址的变量名 
    语法
        数据类型 &别名 = 原名;  别名和原名可以一样
    代码示例：
        int a = 10;
        int &b = a;
        cout << a << endl;
        cout << b << endl;
        b = 30;
        cout << a << endl;
        cout << b << endl;
        cout << &a << endl;
        cout << &b << endl;
        输出：
        10
        10
        30
        30
        0x5ffe74
        0x5ffe74
    ! 1、引用必须初始化 -> 即不能不定原名 / 必须引用一块合法的内存空间
    ! 2、引用初始化后不可更改 在后面用 b=a 是赋值
    
    作函数参数 可改变在函数内实参，比指针简化一点
        代码示例：
            void swap(int &a, int &b){
            int temp = a;
            a = b;
            b = temp;
            }

            int a = 10;
            int b = 20;
            swap (a, b);
            cout << a << "\t" << b << endl;
            输出：
            20      10

    作函数返回值
        ！不要返回局部变量的引用 第二次调用会被清掉
        函数返回地址/引用时,函数的调用课作为左值进行赋值
    引用==*指针     指针==&引用
    引用的本质是 一个指针常量 -> 指向固定地址的指针

    常量引用 用于修饰形参，防止误操作 即将值设定为只读 
        const int&  ref = 10 => int temp = 10; const int& ref = temp;
        

类与对象 - 封装、继承、多态
    万物皆对象 对象有 属性 和 行为
    具有相同性质的对象，我们可以抽象为类

    封装
        意义：
            1、将属性和行为作为整体，表现生活中的事物   
                语法 class 类名{ 访问权限: 属性/行为};
            2、将属性与行为加以权限控制


        